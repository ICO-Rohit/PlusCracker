import React from 'react'
import { Outlet } from 'react-router-dom'

const Indexification = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
}

export default Indexification
