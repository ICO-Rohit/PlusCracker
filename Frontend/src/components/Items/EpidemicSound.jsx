import React from 'react'
import { Outlet } from 'react-router-dom'

const EpidemicSound = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
}

export default EpidemicSound
